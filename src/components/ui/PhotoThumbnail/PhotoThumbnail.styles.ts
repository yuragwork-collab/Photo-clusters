import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    width: 120,
  },
  image: {
    width: 120,
    height: 120,
    borderRadius: 12,
    backgroundColor: "#ddd",
  },
});
